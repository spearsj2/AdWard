[requires]
qt/6.5.0           # Qt6 library (use the latest stable version available)
openssl/1.1.1      # OpenSSL for secure network communication (optional if required)
zlib/1.2.13        # Compression library (used internally by Qt)

[options]
qt:shared=True             # Build Qt as a shared library (dynamic linking)
qt:with_gui=True           # Enable GUI support
qt:with_widgets=True       # Enable Qt Widgets
qt:with_network=True       # Enable Qt Networking
qt:qttools=True            # If you need Qt tools like `qmake` (optional)
openssl:shared=True        # Build OpenSSL as a shared library (optional)

[generators]
cmake_find_package         # Generates `Find<Package>.cmake` files for CMake integration
cmake                      # Generates the CMake build system integration

[tool_requires]
cmake/3.25.1               # CMake for project configuration (ensure it's the latest)
ninja/1.10.2               # Optional: Ninja build system (or use the default Makefile)

[build_requires]
gcc/10.3.0                 # The compiler version (if not using the system default)

[imports]
# Copies DLLs or shared libraries (for Windows or other dynamic linking platforms)
bin, *.dll -> ./bin        # Copies DLL files to the `bin` folder
lib, *.so -> ./lib         